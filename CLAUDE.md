# GenerEdge website — project context

Static rebuild of generedge.com, replacing the WordPress site. Read this before
making changes.

---

## What this is and why

GenerEdge, Inc. is a lending connector in St. Petersburg, Florida. It matches
women- and minority-owned small businesses, and builders, with mission-driven
lenders (CDFIs, credit unions, community banks). **GenerEdge is not a lender.**
That distinction must stay visible in the footer disclaimer and anywhere the
service is described.

The old site was WordPress + Elementor, hosted by a contractor (Isaac Hunter /
IkeHunter Web Development) at $75/month. That arrangement is ending. The site
had 25 published pages, but only about five were real — the rest were dead
landing pages left over from a previous project called Green Energy Fund. The
16 blog posts were all solar-energy content from that era and are deliberately
**not** migrated.

New stack: hand-written static HTML/CSS/JS, deployed to Hostinger. No build
dependencies, no database, no CMS. Same approach as the partner2impact.com site.

Eduardo is the only person who edits this. Tracey Wiseman (CEO) and Steve
Wiseman send content changes to him; they do not edit the site directly.

---

## Repo layout

```
├── src/                    ← EDIT HERE. Page bodies only (no <head>, no nav, no footer).
│   ├── index.html
│   ├── business-loans.html
│   ├── about-us.html
│   ├── contact-us.html
│   ├── privacy-policy.html
│   ├── terms-and-conditions.html
│   ├── cookies-policy.html
│   └── 404.html
├── scripts/
│   ├── build.py            ← assembles src/ + shared chrome → root HTML
│   └── fetch-assets.sh     ← pulls original images off WordPress
├── assets/
│   ├── css/site.css        ← all styles, one file
│   ├── js/site.js          ← nav, reveals, cookie consent, forms
│   └── img/
├── index.html              ← GENERATED. Do not edit by hand.
├── business-loans/index.html   ← GENERATED
├── ... (one directory per page)  ← GENERATED
├── .htaccess               ← legacy 301s, HTTPS, headers (Hostinger/Apache)
├── _redirects              ← same redirects for Netlify/Cloudflare Pages
├── robots.txt / sitemap.xml
```

### The build step

Shared chrome (head, meta, nav, footer, cookie banner) lives in
`scripts/build.py`, not in each page. After editing anything in `src/` or the
`SITE` / `PAGES` dicts in `build.py`:

```bash
python3 scripts/build.py
```

The generated HTML is committed, so deployment is a plain file copy — the
server needs no Python. **Never hand-edit the root `index.html` or the
`*/index.html` files; your change will be overwritten on the next build.**

Placeholders `{{CALENDLY}}`, `{{PHONE}}`, `{{PHONE_HREF}}`, `{{EMAIL}}` are
substituted at build time.

---

## House rules

**Calendly.** Home page uses Steve's link. Every other page uses Tracey's. This
is a standing rule from the client, handled automatically in `build.py` — don't
hardcode Calendly URLs in `src/`.

**Brand.** Forest `#104C2F`, deep `#08281A`, green `#0E9143`, glow `#4CD787`.
Display font Archivo 800, body font Figtree. Defined once as CSS custom
properties at the top of `site.css` — use the variables, never raw hex.

**GenerEdge is not a lender.** Don't write copy that implies it approves,
underwrites, funds, or sets rates. It connects, prepares files, and advocates.

**Content is the client's.** Don't invent statistics, testimonials, client
names, loan volumes, or approval rates. If a number is needed and isn't in this
repo, ask rather than estimate.

---

## Before launch — required

These are blockers. The site should not go live with any of them unresolved.

### 1. Images — real, but two still to design

The five photographs were pulled off the old WordPress install with
`bash scripts/fetch-assets.sh` and are committed. They are the originals, not
placeholders:

| Repo file            | WordPress source                                             |
|----------------------|--------------------------------------------------------------|
| `logo.png`           | `2023/07/logo-color-transparent-800.png`                     |
| `hands-heart.png`    | `2023/10/Hands-Heart-1024x657.png`                           |
| `tracey-wiseman.jpg` | `2026/02/Tracey-New.jpg`                                     |
| `stephen-pool.jpg`   | `2024/08/Stephen-Pool.jpeg`                                  |
| `handshake.jpg`      | `2024/05/57237323-view-of-an-architect-...-site.jpg`         |

Re-running the script re-fetches them, but only while the old host is still up.

`og-default.png` (1200x630 social card) and `apple-touch-icon.png` (180x180)
are generated by `scripts/make-social-images.py` from the real logo and the
brand fonts in `scripts/fonts/`. They are committed, so the site has no build
dependency on Pillow. Re-run that script after a branding change, or replace
its output with hand-designed artwork — nothing else depends on how they look.
`apple-touch-icon.png` and `favicon.svg` draw the same forward chevron as the
logo mark; keep them in step if either changes.

The `width`/`height` attributes on every `<img>` match the real intrinsic size
of the file. If an image is replaced, update those too or the page will shift as
it loads.

Also confirm image licensing — `handshake.jpg` has a stock-photo-style filename
(`57237323-view-of-an-architect...`). If it was a licensed stock photo, confirm
the licence transfers to the new site, or replace it.

### 2. Forms — connected, one activation click outstanding

All three lead forms post to whatever the `FORM` dict in `scripts/build.py`
names. It is set to `formsubmit`, which emails each submission to
`info@generedge.com` with no account, no API key and no server.

**The one thing left:** the first submission FormSubmit receives triggers a
confirmation email to `info@generedge.com` containing an activation link. Until
someone clicks it, submissions are accepted in the browser but not delivered.
Send one test enquiry through the live contact form and click the link.

To move to something else, change `FORM["provider"]` and rebuild — `formsubmit`,
`web3forms` (needs `access_key`), `formspree` (needs `endpoint`), or `custom`
(any URL taking a JSON POST). `assets/js/site.js` handles all four; no JS edit
is needed. Close CRM remains the best end state, via a small serverless endpoint
holding the API key — **the key must never be in this repo or in client-side JS.**

If a send fails for any reason, the form shows the phone number and email
address plus a "Send it by email instead" link that opens the visitor's mail
client pre-filled with everything they typed. A lead is never silently lost.

Forms also include: honeypot field, `aria-invalid` + `role="alert"` inline
errors, `aria-live` status region, disabled-while-sending, and a
`generate_lead` GA event on success.

### 3. Google Analytics is a placeholder

`SITE["ga_id"]` in `build.py` is `G-XXXXXXXXXX`. Replace with the real GA4
measurement ID. Until then `loadAnalytics()` refuses to run — this is
deliberate, so a half-configured site never ships a broken tag.

Analytics **only** load after the visitor presses "Accept analytics". Choosing
"Essential only" downloads no Google script at all. Don't move the GA tag into
the page `<head>` — that would break the consent gate and the Cookie Policy
would become inaccurate.

### 4. Copy that needs Tracey's sign-off

Flagged during the rebuild, listed here so it isn't forgotten:

- **SOC 2 claim.** The old About page said "SOC 2 Type II audited for
  bank-level data security" inside a GenerEdge feature list. That certification
  appears to belong to the Finturf/SafeTurf platform, not to GenerEdge, Inc.
  It has been **rewritten to attribute it to the platform partner** with an
  explicit note that GenerEdge does not claim SOC 2 itself. If GenerEdge *is*
  independently certified, provide evidence and the wording can go back.
- **The "50% lower approval rate" statistic.** The old Business Financing page
  asserted minority- and women-owned businesses are approved "at a rate roughly
  50% lower than non-minority firms", with a bar chart, and no source. It has
  been **replaced with a qualitative statement** and the chart removed. To
  restore it, get a citable source (the Federal Reserve Small Business Credit
  Survey is the usual one) and cite it visibly on the page. Do not publish the
  figure unsourced — this is a financial services site.
- **"Same-day pre-approval" and "initial review within 24 hours"** were on the
  old site as commitments. They have been removed from the stats band and the
  feature list because nobody confirmed GenerEdge can meet them. Restore only
  if Tracey confirms they are accurate.
- **Loan amount ranges contradicted each other** on the old site: the home page
  advertised up to $15M, the Business Financing page said $1M maximum, and its
  own amount dropdown offered "$5M–$15M". Resolved as: Home/builders =
  ADC and permanent financing up to $15M; Business Financing = a separate
  $50K–$1M program, with the dropdown corrected to match. Confirm this is right.

### 5. Legal pages

`privacy-policy`, `terms-and-conditions` carry the real text migrated from
WordPress (effective dates Jan 15 2025 and Dec 1 2025). `cookies-policy` is
**newly written** for this rebuild and describes the consent banner accurately —
if the analytics setup changes, that page must change with it. All three should
get a lawyer's read before launch, since the site collects PII and TCPA SMS
consent.

---

## Domain and DNS — in progress, handled outside this repo

The domain is still controlled by Isaac. Registrar is a Tucows reseller
(WHOIS shows TUCOWS.COM CO.); paid through January 2027; Hostinger confirms it
is transfer-eligible.

**The critical constraint:** GenerEdge email runs on generedge.com through
Google Workspace. The MX, SPF, DKIM and DMARC records live in the DNS zone
Isaac controls. Moving nameservers without replicating those records exactly
kills company email and burns the domain's sending reputation, which also
breaks the outbound campaigns Eduardo runs.

Order of operations:

1. Get a full DNS zone export from Isaac.
2. Recreate the zone at Hostinger — **MX, SPF, DKIM, DMARC first.**
3. Switch nameservers. **Verify mail flows in and out before continuing.**
4. Then transfer the registrar (~$11, includes a year of renewal).
5. Then point the site at this repo.

When setting the domain up in Hostinger, **decline its email offer.** The setup
wizard will write Hostinger's own MX records and silently overwrite Google
Workspace.

---

## Deploying

### GitHub Pages — automatic

`.github/workflows/deploy-pages.yml` builds and publishes on every push to the
repository's default branch. Pushes to other branches run the build and the
checks as CI but publish nothing.

Live at <https://ejcorral3s.github.io/Generedge-Website/>.

Pages serves from the **`gh-pages`** branch, which holds generated output only.
**Never edit `gh-pages` by hand** — the next deploy overwrites it wholesale.
Publishing that way is deliberate: pushing a branch needs only `contents: write`,
which a workflow can grant itself, whereas `actions/deploy-pages` needs Pages to
have been enabled by an account with admin rights. The first push of a
`gh-pages` branch is what switched Pages on in the first place.

A project site is served from `https://<user>.github.io/<repo>/`, not a domain
root, so the workflow derives that sub-path from the repository name and builds
with `GE_BASE` set to it. `apply_base()` in `build.py` rewrites every
root-absolute `href`/`src`/`action` to match. **This is why nothing in `src/` may
hardcode an origin** — write `/about-us/`, never `https://generedge.com/about-us/`,
or it will not be rewritten and will break on Pages.

Any build whose origin ends in `.github.io` is emitted `noindex, nofollow`
(override with `GE_NOINDEX`). Do not remove this: a project site's robots.txt is
published at a path crawlers never fetch, so the meta tag is the only thing
stopping a self-canonicalising duplicate of a financial services site from
competing with generedge.com in search.

Attaching the real domain later: add a `CNAME` file with `generedge.com` at the
repo root and set the domain in Settings -> Pages. `GE_BASE` then resolves to
empty, URLs return to domain-root form and the pages become indexable, with no
other change.

### Hostinger

The committed root-level HTML is built for a domain root, so deployment is a
plain file copy. Connect the repo under Websites -> Advanced -> Git, or upload
the contents of `_site/` (`python3 scripts/package.py`) to `public_html/`.

`.htaccess` must be uploaded — it carries the legacy 301s that preserve SEO,
plus HTTPS enforcement and security headers. GitHub Pages ignores it; the
redirects only take effect on the Apache/LiteSpeed host.

### Build configuration

| Variable | Default | Purpose |
|---|---|---|
| `GE_ORIGIN` | `https://generedge.com` | host used for canonicals, Open Graph and the sitemap |
| `GE_BASE` | *(empty)* | sub-path the site is served under |

Always re-run `python3 scripts/check.py` after a build. With `GE_BASE` set it
also asserts that no internal URL escaped the prefix.

## Accessibility baseline — keep it

Established during the rebuild; don't regress it:

- Skip link to `#main`; one `<h1>` per page; headings in order.
- All images have descriptive `alt`; decorative SVG has `aria-hidden`;
  informative SVG has `role="img"` + `aria-label`.
- Every form control has a real `<label>`, `autocomplete`, and inline errors.
- Focus is always visible (`:focus-visible`, 3px outline).
- `--muted` was darkened from `#5B6C62` to `#4A5A51` to clear WCAG AA on the
  stone background. Don't lighten it.
- The fee chart is decorative (`aria-hidden`) and backed by a visually-hidden
  `<table>` that screen readers get instead.
- Mobile nav is a real disclosure button with `aria-expanded` / `aria-controls`.
  The old prototype simply hid the links under 820px, leaving phones with no
  navigation at all.
- `prefers-reduced-motion` disables animation and reveals content immediately.
